import * as React from 'react';
import {  Text } from 'react-native';
import {useSelector,useDispatch} from 'react-redux';

function ceatrUsers() {
    const url = useSelector(state => state.url);
   return (

/*     <>
    <Text>HOLL {url}</Text>
    </>
 */
    console.log('asdas')

  ); 

} 

export default ceatrUsers;